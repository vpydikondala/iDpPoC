apiVersion: v1
kind: Service
metadata: { name: {{ include "{{name}}.fullname" . }} }
spec:
  type: {{ .Values.service.type }}
  ports: [ { port: {{ .Values.service.port }}, targetPort: 8080, name: http } ]
  selector: { app.kubernetes.io/name: {{ .Chart.Name }} }
