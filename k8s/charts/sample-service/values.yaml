replicaCount: 1
image: { repository: ghcr.io/example/sample-service, tag: latest, pullPolicy: IfNotPresent }
service: { type: ClusterIP, port: 80 }
resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits:   { cpu: 200m, memory: 256Mi }
